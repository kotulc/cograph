/**
 * Re-exports of the internal element builder functions from useGraphModel
 * so they can be tested without React/hook machinery.
 */

export { graphElements, maxDepthBelow, projectRoot, langColor } from '../../src/useGraphModel.js';
